apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "chidrixx-controlplane.fullname" . }}
  labels:
    {{- include "chidrixx-controlplane.labels" . | nindent 4 }}
spec:
  # SQLite is single-writer — this must never be scaled beyond 1 replica
  # without moving the store to something server-based first.
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: chidrixx-controlplane
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: chidrixx-controlplane
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: controlplane
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - -addr=:{{ .Values.service.port }}
            - -db=/data/controlplane.db
          {{- if .Values.auth.tokenSecretName }}
          env:
            - name: CHIDRIXX_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.auth.tokenSecretName }}
                  key: {{ .Values.auth.tokenSecretKey }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 2
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "chidrixx-controlplane.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
