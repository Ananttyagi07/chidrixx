{{- if and .Values.auth.generate (not .Values.auth.tokenSecretName) }}
{{- $secretName := printf "%s-auth-token" (include "chidrixx-controlplane.fullname" .) }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- $token := "" }}
{{- if $existing }}
{{- $token = index $existing.data .Values.auth.tokenSecretKey | b64dec }}
{{- else }}
{{- $token = randAlphaNum 32 }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "chidrixx-controlplane.labels" . | nindent 4 }}
  annotations:
    # helm diff/lint on a `lookup`-derived value can otherwise show a
    # spurious change every render; this is here for humans reading the
    # object, not read by the chart itself.
    chidrixx.dev/generated: "true"
type: Opaque
data:
  {{ .Values.auth.tokenSecretKey }}: {{ $token | b64enc }}
{{- end }}
